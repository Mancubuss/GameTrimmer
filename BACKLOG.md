# Беклог GameTrimmer

Живий перелік запланованих змін. Оновлено: **2026-07-15**.
Порядок у межах розділу ≈ пріоритет. Виконане переноситься вниз, у «Зроблено».

## В роботі

_(порожньо)_

## Заплановано — Фаза 4

1. **Налаштовуваний keep-list мов у UI** — зараз жорстко uk+en у `LangDetector::new`.
2. **Карантин-режим** — переміщення видаленого у сховок з можливістю відновлення,
   замість Кошика (точка розширення: трейт `Remover` у `core/src/ops.rs`).
3. **Налаштування маршрутизації сканування** — авто / примусово MFT / примусово walkdir
   (зараз авто: HDD→MFT, SSD→walkdir; виміряно ~40× на користь walkdir на SSD).
4. **Пакування** — портабельний пакет (exe + rules.json + README), іконка через winres,
   перевірка DPI-масштабування.

## Заплановано — після Фази 4

5. **Інкрементальне сканування через USN Change Journal** — журнал змін NTFS замість
   повторного повного проходу: per-volume USN-курсори в БД, оновлення лише зачеплених
   ігор; обовʼязковий фолбек на повний скан (журнал вимкнений/переповнений/курсор
   застарів). Потребує елевації — шлях уже є. (З обговорення 2026-07-15: наївний
   diff проти зрізу в БД не економить головного — перелічування файлів.)
6. **Модалка елевації — показувати рідше** — не пропонувати UAC-перезапуск, якщо всі
   бібліотеки на SSD (MFT-шлях там і так не використовується). Робити після п. 3.
7. **Уточнення згортання тек** — за прикладами помилкових згрупувань від користувача;
   можливо, послабити правило (частково позначені теки локалізацій зараз НЕ згортаються).
   Статус: чекає прикладів.

## Технічний борг / відомі хвости

- `binrw` 0.11.3 future-incompat — транзитивна залежність через крейт `ntfs`;
  чекає релізу `ntfs`.
- Корпус локалізацій: 31 kind-mismatch (audio→video); recall движка 77% —
  свідома ціна безпеки, переглядати разом із keep-list UI.

## Зроблено — MVP подання даних (коміти ddab4c7…07e94f5, 2026-07-15)

- **2026-07-15, завантаження попереднього зрізу при старті** — якщо в базі є
  збережені findings, програма показує їх одразу після запуску (фоновий воркер
  `worker/load.rs`; `group_dir` переобчислюється, невідомі категорії пропускаються);
  «Сканувати бібліотеки» оновлює зріз як і раніше.
- **2026-07-15, переробка подання даних** (за 7 зауваженнями):
  дерево диск → гра → категорія → тека/файл (ігри не дублюються); ігри й теки
  згорнуті за промовчанням; колонки з вирівнюванням (Назва | Мова | Файлів |
  Розмір | Довіра); пояснення правила — у tooltip; ⚠ для впевненості < 85%;
  клавіатурна навігація (↑↓, PgUp/PgDn, Home/End, →/←, Space); масовий вибір
  (кнопки «Вибрати все»/«Зняти вибір», ПКМ-меню на диску/грі/категорії, зокрема
  «категорія на всьому диску»); ⓘ-пояснення принципу автовибору.
- **2026-07-15, виправлення категоризації VCRedist** — опційне `max_depth` на
  правило в `rules.json` (глибокі `Support\Software\VCRedist\` тепер redist,
  не «бонусні матеріали»); патерни `vc_?redist`, додано `dxsetup.exe`.
- **2026-07-15, сортування** — теки в категорії за розміром (спадно), окремі
  файли та вміст тек — за шляхом/іменем; виправлено хаотичний порядок вмісту
  тек із MFT-шляху.
- **2026-07-15, CSV-експорт** — кнопка «Експортувати…», UTF-8 BOM, `;`, CRLF.

## Зроблено — раніше

- Епопея прискорення сканування (4 коміти, 2026-07-15): MFT-індексатор у стилі
  Everything, потокове читання $MFT + FRN-зіставлення, елевація та маршрутизація
  за типом носія, вилучення C++/Qt легасі з master (архівні гілки `legacy/*`).
