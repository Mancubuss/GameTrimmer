# GameTrimmer

GameTrimmer — це утиліта для очищення ігрових бібліотек (Steam, Epic, GOG, Ubisoft, EA) від непотрібних файлів. Програма допомагає знайти та безпечно видалити:
- Редистрибутиви (vcredist, directx, тощо)
- Зайві локалізації (зі збереженням keep-list мов)
- Документацію та файли довідки
- Інші допоміжні файли, які не потрібні для запуску ігор

## Можливості

- Автоматичне виявлення бібліотек Steam/Epic/GOG/Ubisoft/EA + ручні теки
- Швидке сканування: прямий парсинг $MFT (NTFS) на HDD, walkdir на SSD
- Кеш результатів у SQLite
- Движок правил локалізацій із запобіжниками проти хибних спрацювань
- Видалення лише за явним підтвердженням користувача
- Темна тема, High-DPI, один портабельний exe

## Стек

Rust + egui (eframe), rusqlite, walkdir + rayon. Воркспейс:
- `crates/core` — сканування, правила, кеш
- `crates/app` — GUI

## Збірка

```
cargo build --release
```

> Примітка: збірка йде в зовнішній target-каталог (див. `.cargo/config.toml`) через кирилицю в шляху проєкту.

## Пакети правил

Аналізатор керується двома JSON-файлами в корені репозиторію:

- [`rules.json`](rules.json) — правила категорій (редистрибутиви, документація,
  бонусні матеріали тощо): патерн шляху → категорія → впевненість.
- [`l10n_rules.json`](l10n_rules.json) — дані движка виявлення локалізацій:
  словник мов (канонічні ключі та їхні аліаси за рівнем довіри), маркерні слова
  (аудіо/текст/відео/шрифти, негативні маркери) та типовий keep-list мов.

Обидва файли — канонічна частина репозиторію (не згенеровані артефакти) і
вбудовуються в exe на етапі збірки. Це водночас і сирці програми, і окремі
завантажувані артефакти: їх можна звантажити прямо з GitHub незалежно від
самої програми за прямими посиланнями (raw, гілка `master`):

```
https://raw.githubusercontent.com/Mancubuss/GameTrimmer/master/rules.json
https://raw.githubusercontent.com/Mancubuss/GameTrimmer/master/l10n_rules.json
```

### Як імпортувати пакет правил

1. Звантажте потрібний файл (свій набір правил, доповнений спільнотою словник
   мов, тощо) — формат визначається автоматично за структурою JSON, тому назва
   файлу значення не має.
2. У застосунку відкрийте **Налаштування → Імпортувати правила** і виберіть
   звантажений файл (можна вибрати кілька одразу).
3. Застосунок зливає імпортований пакет із поточним: нові правила/мови/слова
   додаються, збіги (та сама категорія+патерн, той самий ключ мови) оновлюються
   новими даними з пакета — **нічого не видаляється**. Перед записом поточний
   файл автоматично копіюється як `*.bak`.
4. Зміни діють з наступного сканування.

Кнопка **Налаштування → Експортувати правила** зберігає поточний ефективний
набір обох файлів у вибрану теку — зручна відправна точка для власних правок
або для пропозиції змін спільноті.

## Історія

Попередні спроби збережено в архівних гілках: `legacy/cpp-qt` (C++20 + Qt 6.8), `legacy/winapi-cpp` (C++/WinAPI), `legacy/python` (перший прототип).

## Ліцензія

MIT License
