import json
import subprocess
import urllib.request
import urllib.error
import sys

sys.stdout.reconfigure(encoding='utf-8')

# 1. Get decrypted token from DPAPI via powershell
ps_cmd = """
Add-Type -AssemblyName System.Security
$bytes = [System.IO.File]::ReadAllBytes("E:\\Mancubus\\Projects\\Vibecoding\\Vikunja\\google-antigravity-agent.api-token.dpapi")
$unprotected = [System.Security.Cryptography.ProtectedData]::Unprotect($bytes, $null, [System.Security.Cryptography.DataProtectionScope]::CurrentUser)
[System.Text.Encoding]::UTF8.GetString($unprotected)
"""

res = subprocess.run(["powershell", "-NoProfile", "-Command", ps_cmd], capture_output=True, text=True, check=True)
token = res.stdout.strip()
if not token:
    raise RuntimeError("Failed to obtain decrypted Vikunja API token.")

headers = {
    "Authorization": f"Bearer {token}",
    "Content-Type": "application/json; charset=utf-8",
    "Accept": "application/json"
}

project_id = 5

epic_desc = """# Епік GT-EP16: Модульна зовнішня система локалізації UI (Community-Driven External Localization & i18n Engine)

- **Батьківський проект:** GameTrimmer (v1.1.0+)
- **Статус епіку:** Затверджено до реалізації / До виконання
- **Головна мета:** Повне винесення всіх інтерфейсних рядків та динамічних повідомлень за межі скомпільованого бінарного exe-файлу у відкриті файли локалізації (JSON / Fluent), що дозволить спільноті виправляти помилки, адаптувати термінологію та самостійно додавати нові мови без перезбирання програми з вихідного коду.

---

### Архітектурна концепція та ключові принципи:
1. **Zero-Broken-UI Guarantee (Незламний UI):** Всі базові переклади (англійська та українська) залишаються вбудованими у бінарник (`include_str!`) як бейзлайн. Якщо зовнішній файл відсутній, пошкоджений або містить синтаксичну помилку, рушій прозоро деградує до вбудованої мови без падіння процесу.
2. **Cascading Key Fallback:** Якщо у користувацькій локалізації (наприклад, польській чи німецькій) відсутній якийсь конкретний ключ або додано нову фічу у свіжій версії програми, цей конкретний ключ підтягується з англійського базового пакета.
3. **Pluralization & Smart Interpolation:** Підтримка граматичних форм множини (one, few, many, other) для слов'янських та складних мов, а також безпечна інтерполяція змінних (`{count}`, `{path}`, `{err}`).
4. **Dynamic Discovery & Hot-Reload:** Автоматичне сканування директорії `locales/` поруч із `.exe` (та у `%LOCALAPPDATA%`), динамічне наповнення селектора мов у налаштуваннях та миттєве перемикання/перезавантаження на льоту без перезапуску програми.
5. **Community-Friendly Tooling:** Надання JSON-Schema для автодоповнення у VS Code, кнопок «Експорт шаблону перекладу» та «Відкрити теку локалізацій» прямо в інтерфейсі, а також вбудованого CLI/тестового валідатора цілісності перекладів.

---

### Склад завдань епіку (GT-188 – GT-195):
1. **GT-188:** [Спайк] Архітектура та вибір формату зовнішньої локалізації (Fluent vs Structured JSON/ICU vs TOML).
2. **GT-189:** [Спайк] Модель відмовостійкості, каскадний Fallback та Zero-Panic деградація.
3. **GT-190:** [Ядро] Динамічний `LocaleRegistry`, перехід від закритого `enum Lang` до відкритого `LocaleId`.
4. **GT-191:** [Ядро] Рушій завантаження перекладів, інтерполяція параметрів та плюралізація.
5. **GT-192:** [UI/UX] Динамічний селектор мов у налаштуваннях, експорт шаблонів та Hot-Reload.
6. **GT-193:** [Типографіка] Глобальні шрифтові ланцюжки (Unicode coverage) та адаптивність розмірів UI-елементів.
7. **GT-194:** [Спільнота & QA] JSON-Schema валідація, CLI-лінтер перекладів та перевірка відсутніх ключів у тестах.
8. **GT-195:** [Міграція] Екстракція `en`/`uk` у канонічні зовнішні пакети, ретрофіт `i18n` модулів та регресійні тести.
"""

tasks = [
    {
        "title": "GT-EP16 · Модульна зовнішня система локалізації UI (Community-Driven External Localization & i18n Engine)",
        "priority": 4,
        "description": epic_desc
    },
    {
        "title": "GT-188 · [Спайк: Архітектура та вибір формату зовнішньої локалізації] Порівняльний аналіз Fluent (.ftl), Structured JSON та TOML: плюралізація, швидкість парсингу, простота для спільноти та відповідність філософії GameTrimmer",
        "priority": 3,
        "description": """# Спайк GT-188: Архітектура та вибір формату зовнішньої локалізації

- **Мета:** Визначити оптимальний формат зовнішніх файлів локалізації, що поєднує простоту редагування для звичайних користувачів із потужністю граматичних правил.
- **Предмет дослідження:**
  1. **Fluent (Mozilla Project Fluent / .ftl):**
     - *Плюси:* Неперевершена підтримка граматики, роду, складних форм множини (CLDR Plural Rules); ізоляція помилок перекладача від логіки коду.
     - *Мінуси:* Додаткова залежність `fluent-bundle`, специфічний синтаксис для авторів без досвіду.
  2. **Structured JSON (з підтримкою ключів множини):**
     - *Плюси:* Нульові нові залежності (використання вже інтегрованого `serde_json`), максимальна простота редагування будь-яким редактором, легка валідація через JSON Schema, нативна підтримка у веб-платформах перекладів (Crowdin, Weblate).
     - *Мінуси:* Необхідність реалізації власної легкої логіки вибору форми множини (`one`, `few`, `many`, `other`) для мов із складним відмінюванням.
  3. **TOML:**
     - *Плюси:* Зручність для ручного читання конфігів.
     - *Мінуси:* Менш стандартний для перекладацьких сервісів порівняно з JSON/PO.
- **Очікуваний результат спайку:**
  - Архітектурний звіт із бенчмарками парсингу, розміром бінарника та фінальною рекомендацією формату (або гібридного JSON-формату з розширеним маніфестом).
"""
    },
    {
        "title": "GT-189 · [Спайк: Модель відмовостійкості, каскадний Fallback та Zero-Panic деградація] Проєктування трирівневої системи захисту інтерфейсу при пошкоджених, неповних або несумісних файлах перекладу",
        "priority": 3,
        "description": """# Спайк GT-189: Модель відмовостійкості та каскадний Fallback

- **Мета:** Гарантувати, що будь-яка помилка користувача у зовнішньому файлі (синтаксична помилка, пропущений ключ, зламаний UTF-8, застаріла версія схеми) ніколи не призведе до паніки програми (panic=abort/unwind) або порожніх написів у вікні.
- **Ключові технічні задачі:**
  1. **Каскадна модель Fallback-ланцюжка:**
     - `Рівень 1 (Active Custom)`: Значення з активного зовнішнього файлу `locales/<lang>.json`.
     - `Рівень 2 (Embedded Native)`: Якщо файл зовнішній, але це `uk` або `en` — брати з вбудованого бінарного ресурсу цієї мови.
     - `Рівень 3 (Embedded English Baseline)`: Якщо мова стороння (наприклад, `pl`/`de`), а ключа немає — автоматично віддавати англійський вбудований еквівалент.
     - `Рівень 4 (Key Literal)`: Якщо ключ відсутній скрізь (критична помилка версій) — повертати рядок `"[missing: key_name]"` з логуванням у діагностичний журнал.
  2. **Абсолютна безпека інтерполяції:**
     - Захист від паніки `format!` при невідповідності кількості плейсхолдерів `{0}`, `{count}`, `{path}` у користувацькому рядку.
  3. **Діагностичне логування помилок парсингу:**
     - Виведення точних номерів рядків та деталей помилок парсингу у `gametrimmer.log` без блокування запуску UI.
"""
    },
    {
        "title": "GT-190 · [Ядро: Динамічний LocaleRegistry та перехід від закритого enum Lang до відкритого LocaleId] Рефакторинг crates/core/src/settings.rs та створення відкритого реєстру мов",
        "priority": 4,
        "description": """# Завдання GT-190: Динамічний LocaleRegistry та абстракція LocaleId

- **Мета:** Відв'язати ядро та конфігурацію від жорстко зашитого `enum Lang { En, Uk }`, створивши динамічну модель локалей, що підтримує довільні ISO/BCP-47 коди.
- **Компоненти розробки:**
  1. **Структура `LocaleId`:**
     - Компактний, клонований ідентифікатор мови (на основі `SmolStr` або `Arc<str>`).
     - Константи `LocaleId::EN` ("en") та `LocaleId::UK` ("uk") для швидкого зіставлення у внутрішніх підсистемах.
  2. **Маніфест мовного пакета (`LocaleMeta`):**
     - Поля: `id` ("pl"), `name_english` ("Polish"), `name_native` ("Polski"), `author` ("Community"), `version` ("1.0.0"), `target_version` ("1.1.0"), `plural_rule` ("slavic"), `rtl` (bool).
  3. **Реєстр `LocaleRegistry` у `crates/core`:**
     - Пошук та реєстрація встановлених пакетів у двох місцях:
       - Пріоритет 1: Директорія `locales/` поруч із `gametrimmer.exe` (Portable mode).
       - Пріоритет 2: `%LOCALAPPDATA%\\GameTrimmer\\locales\\` (Installed mode).
     - Вбудовані незламні маніфести для `en` та `uk`.
  4. **Оновлення `gametrimmer.ini`:**
     - Збереження `app_language = pl` замість фіксованих `en`/`uk`, збереження підтримки `app_language = system`.
"""
    },
    {
        "title": "GT-191 · [Ядро: Рушій завантаження перекладів, інтерполяція параметрів та плюралізація] Реалізація універсального i18n рушія з підтримкою граматичних форм",
        "priority": 4,
        "description": """# Завдання GT-191: Локалізаційний рушій та система плюралізації

- **Мета:** Забезпечити швидкий доступ до рядків під час рендерингу (O(1) доступ) та коректну генерацію динамічних повідомлень з формами множини.
- **Компоненти розробки:**
  1. **Структура сховища перекладів у пам'яті:**
     - Швидка хеш-таблиця `AHashMap<StringId, String>` або індексований вектор для статичних рядків інтерфейсу.
     - Кешування активного шару перекладу при старті та зміні налаштувань (zero allocation під час покадрового рендерингу egui).
  2. **Рушій плюралізації (Plural Rules Engine):**
     - Реалізація стандартних CLDR категорій: `one`, `few`, `many`, `other`.
     - Підтримка специфіки української/польської мов (1 файл, 2-4 файли, 5-20 файлів, 21 файл...).
     - Підтримка романських та германських мов (1 file / 2+ files).
  3. **Безпечний рушій заміни плейсхолдерів:**
     - Заміна іменованих параметрів `{count}`, `{size}`, `{path}`, `{error}` без ризику паніки при невідповідності типів чи відсутності аргументів.
  4. **Двомовне розділення `Reported`:**
     - Збереження філософії GameTrimmer: системні логи завжди ведуться англійською для полегшення діагностики, а інтерфейс відображається активною мовою.
"""
    },
    {
        "title": "GT-192 · [UI/UX: Динамічний вибір мови, експорт шаблонів та Hot-Reload] Оновлення розділу налаштувань General & Rules для керування сторонніми перекладами",
        "priority": 4,
        "description": """# Завдання GT-192: Інтеграція вибору мов та інструментів перекладу в UI

- **Мета:** Надати користувачам зручний інтерфейс для вибору встановлених мов та створення власних локалізацій без виходу з програми.
- **Компоненти розробки:**
  1. **Динамічний ComboBox мов у `crates/app/src/ui/settings/general.rs`:**
     - Автоматичне заповнення спадного списку всіма виявленими локалізаціями з їхніми самоназвами (наприклад: *English*, *Українська*, *Polski*, *Deutsch*, *Français*, *日本語*).
     - Збереження опції *«Системна (відповідає Windows)»* з автоматичним зіставленням BCP-47 коду Windows до наявних пакетів.
  2. **Інструменти для авторів перекладів у налаштуваннях:**
     - Кнопка *«Відкрити теку локалізацій»* (відкриває `locales/` у Провіднику Windows).
     - Кнопка *«Експорт шаблону перекладу (.json)»* (створює актуальний файл `gametrimmer.template.json` з усіма актуальними ключами та англійським текстом).
     - Кнопка *«Перезавантажити мовні пакети (Hot-Reload)»* (миттєве оновлення тексту без перезапуску застосунку).
  3. **Візуальний бейдж статусу пакета:**
     - Позначка для сторонніх / користувацьких перекладів (Community Pack 🌐) та відсоток заповненості ключів (наприклад: *Polski · 100%* або *Español · 94%*).
"""
    },
    {
        "title": "GT-193 · [Типографіка та UI-адаптація: Шрифтові ланцюжки та еластичність верстки] Забезпечення глобального Unicode-покриття та запобігання обрізанню тексту різної довжини",
        "priority": 3,
        "description": """# Завдання GT-193: Шрифтові ланцюжки та еластичність UI

- **Мета:** Запобігти появі «квадратів» (tofu glyphs) для східноєвропейських, азійських чи інших шрифтів та забезпечити бездоганне відображення інтерфейсу різними мовами.
- **Компоненти розробки:**
  1. **Розширення ланцюжка шрифтів у `crates/app/src/main.rs`:**
     - Аудит системних шрифтів Windows для покриття розширеної кирилиці, латиниці (Latin Extended-A/B: польська, чеська, турецька), грецької, арабської мов.
     - Fallback-ланцюжок: `Segoe UI` -> `Segoe UI Symbol` -> `Microsoft YaHei` (CJK) -> `Malgun Gothic` (Корейська) -> `Segoe UI Historic` / `Arial Unicode MS`.
  2. **Аудит довжини рядків (UI Text Expansion):**
     - Відомо, що тексти німецькою, французькою чи польською мовами можуть бути на 25–40% довшими за англійські оригінали.
     - Аудит усіх кнопок, заголовків таблиць, модальних вікон та плашок на предмет автоматичного перенесення (`ui.label(egui::RichText::new(...).wrap())`) та мінімальної фіксованої ширини колонок.
  3. **Підготовка до RTL (Right-to-Left) вирівнювання (майбутня сумісність):**
     - Врахування прапорця `rtl` у метаданих для коректного вирівнювання тексту.
"""
    },
    {
        "title": "GT-194 · [Інструменти спільноти: JSON-Schema, CLI-валідатор та лінтер перекладів] Створення схеми валідації, автоматичних тестів повноти та CLI команди перевірки",
        "priority": 3,
        "description": """# Завдання GT-194: Схема валідації, автоматичні тести та CLI лінтер

- **Мета:** Дати спільноті та CI автоматизований інструмент перевірки якості перекладів на наявність помилок до злиття в репозиторій.
- **Компоненти розробки:**
  1. **Створення `locales/schema/locale.schema.json`:**
     - Офіційна JSON Schema з описом усіх обов'язкових ключів, типів даних, параметрів інтерполяції та структури метаданих.
     - Підтримка валідації та автодоповнення прямо в VS Code / JetBrains / Cursor.
  2. **CLI команда валідації перекладів:**
     - `gametrimmer --verify-locale <path_or_dir>`: перевірка валідності JSON, відповідності схемі, пошук неперекладених ключів та зайвих застарілих рядків.
  3. **Автоматичні тести у `cargo test`:**
     - Тест `test_all_packaged_locales_are_complete_and_valid`: гарантує, що всі вбудовані та офіційні мовні пакети мають 100% покриття ключів та валідні плейсхолдери.
"""
    },
    {
        "title": "GT-195 · [Міграція та ретрофіт: Експорт базових en/uk пакетів та інтеграційні тести] Повний перехід кодової бази на нову систему та відключення зашитих рядків",
        "priority": 4,
        "description": """# Завдання GT-195: Міграція існуючих рядків та підсумкове тестування

- **Мета:** Завершити міграцію всіх існуючих рядків `en.rs`, `uk.rs` та `messages.rs` у нові файли `locales/en.json` та `locales/uk.json`, оновити всі виклики в UI та верифікувати працездатність.
- **Компоненти розробки:**
  1. **Генерація еталонних файлів `locales/en.json` та `locales/uk.json`:**
     - Повна міграція понад 200+ статичних ключів та 40+ динамічних функцій повідомлень.
  2. **Оновлення викликів у `crates/app`:**
     - Заміна викликів `i18n::strings(lang).field` на звернення до актуального рушія перекладів.
     - Забезпечення зворотної сумісності або чистого рефакторингу модуля `crates/app/src/i18n`.
  3. **UI та Harness тестування:**
     - Запуск повного набору тестів `cargo test`.
     - Тестування в `egui_kittest` для перевірки динамічного перемикання мови наживо та рендерингу стороннього пакету.
"""
    }
]

print(f"Creating Epic and {len(tasks)-1} tasks on Vikunja board (Project ID: {project_id})...")

created_ids = []

for idx, t in enumerate(tasks):
    payload = {
        "title": t["title"],
        "description": t["description"],
        "priority": t["priority"]
    }
    data = json.dumps(payload, ensure_ascii=False).encode('utf-8')
    req = urllib.request.Request(f"http://127.0.0.1:3456/api/v2/projects/{project_id}/tasks", data=data, headers=headers, method="POST")
    try:
        with urllib.request.urlopen(req) as resp:
            res = json.loads(resp.read().decode('utf-8'))
            tid = res.get('id')
            tidx = res.get('index')
            created_ids.append((tid, tidx, t["title"]))
            print(f"Created task [{idx+1}/{len(tasks)}]: ID {tid}, Index {tidx} -> {t['title'][:65]}")
    except urllib.error.HTTPError as e:
        err = e.read().decode('utf-8')
        print(f"Error creating task {idx+1}: HTTP {e.code} - {err}")
        sys.exit(1)

# Link subtasks to epic
epic_id = created_ids[0][0]
child_ids = [c[0] for c in created_ids[1:]]

print(f"\nLinking {len(child_ids)} child tasks to Epic ID {epic_id}...")

for cid in child_ids:
    payload = {
        "other_task_id": cid,
        "relation_kind": "subtask"
    }
    data = json.dumps(payload).encode('utf-8')
    req = urllib.request.Request(f"http://127.0.0.1:3456/api/v1/tasks/{epic_id}/relations", data=data, headers=headers, method="PUT")
    try:
        with urllib.request.urlopen(req) as resp:
            res = json.loads(resp.read().decode('utf-8'))
            print(f"Linked child {cid} to epic {epic_id}")
    except urllib.error.HTTPError as e:
        err = e.read().decode('utf-8')
        print(f"Error linking {cid}: HTTP {e.code} - {err}")

print("\n--- ALL TASKS AND RELATIONS CREATED SUCCESSFULLY IN VIKUNJA ---")
